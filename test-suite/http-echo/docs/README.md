# Datapath comparisons across CNIs

This folder captures datapath notes for the echo-server / curl-client setup:

- Two worker nodes, pod-to-pod both same-node and cross-node.
- ClusterIP service added to see the service/LB path.

Per-CNI details are in the individual files.
